import { ShowClosePasswordDirective } from './show-close-password.directive';

describe('ShowClosePasswordDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowClosePasswordDirective();
    expect(directive).toBeTruthy();
  });
});
